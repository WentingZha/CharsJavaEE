server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zhadb?serverTimezone=UTC
    username: root
    password: Abc@1234

mybatis-plus:
  global-config:
    db-config:
      id-type: auto #id is auto_increment
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
